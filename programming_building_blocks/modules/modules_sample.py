import math
import datetime
import random
#This shows all function that contains within the math modles.
print(dir(math))

print(datetime.date.today())

print(math.sqrt(81))

frits = ["jiuce","apple","mango","pineapple","orange"]
choose = random.choice(frits)
print(choose)

#This is just a list of built in modules 
#we can use and we can also make our own uuser modules too

